# バックログ（タスク・要望の回収キュー）

実装タスクとステークホルダーの要望をここに1件ずつ積み、パイプラインを流す入口。`/loop-review` の intake はここを読み、`/pr-watch` の消化もここを読む。
**優先度と「やる/やらない」は人間が決める。** AI は仕分けと消化を担う。

> **このファイルの役割＝キューだけ。**
> **設計の議論と決定はリポジトリ内の docs/ に置く。**
> - 業務ルール・受入基準 → `docs/design/domains/<domain>/業務ルール.md`
> - ドメインモデル → `docs/design/domains/<domain>/ドメインモデル.md`
> - アーキテクチャ・技術選定の判断 → `docs/adr/<backend|frontend>/NNNN-*.md`
> - 未解決の設計論点 → `docs/design/qa/*.md`
>
> ここには「誰が何を言ったか」「今どの状態か」「着手できるか」しか書かない。
> 迷ったら: **決めた理由・トレードオフ・図 → docs/** ／ **順番と状態 → ここ**。

## 使い方

1. **受領** — タスクや意見が出たら1行追加する。要望は **誰の・どんな言葉か（引用ベース）** で書く（捏造しない）。
2. **仕分け** — kakeibo-claude をカレントに Claude Code を起動し `/loop-review`。分類・優先・ゲートを埋め、**設計の議論は docs/ の該当ファイルへ書く**。
3. **消化** — 1ユースケース／1画面 ＝ 1ブランチ(`feat/…`) ＝ 1 Draft PR。実装は `/impl-api` `/impl-front` が回す。証拠（lint・test・CI・必要なら browser-qa のスクショ）を PR に貼る。
4. **承認** — 人間が Draft を Ready にして merge。merge を検知して `/pr-watch` が次の1件に着手する。

## ステータス

`未仕分け → 未着手 → 開発中 → レビュー中(Draft PR) → merged` ／ 別枠 `保留` `見送り`

## 分類

| 分類 | 意味 |
|---|---|
| **機能** | ループ化して作る（コード変更を伴う） |
| **ルール** | CLAUDE.md・skill・rules・memory の整備で足りる |
| **人間** | 業務ルール・優先度そのもの（本人／利用者が決める） |

## 着手ゲート

**着手には2つが揃う必要がある。混同しない**:

1. **優先** 欄 — **やると決まっているか**。`未設定` の間は着手しない。**優先度は人間が決める**
2. **ゲート** 欄 — **設計の疑問が残っていないか**

ループ（`/pr-watch`）の判定:

- 優先が `未設定` → **着手せず停止**し、優先度の決定を促す
- 優先あり ＋ ゲート **なし** → 確認せずそのまま着手する（着手のたびに人を止めない）
- 優先あり ＋ ゲート **あり** → 着手せず停止し、その点を**まとめて**質問する

> **「ゲートなし」は「着手可」ではない。** 設計の疑問が無いことと、やると決まっていることは別。

ゲートは本来 `/loop-review` の仕分けで潰しておくもの。**聞く場所は仕分け、走る場所は着手**に分ける。

---

## 実装キュー

*実装フェーズのタスク。取り込み元は [機能一覧](docs/design/機能一覧.md)（全 26 操作＝API 単位）と [画面一覧](docs/design/画面一覧.md)（全 19 画面＝フロント単位）。*
*キューに載せるのは「これから着手するもの」だけ。全件をここに写さない（一覧性は上記2ファイルが持つ）。*

| ID | 対象（機能 # / 画面） | 種別 | 優先 | ステータス | ゲート | 設計 |
|----|----|----|----|----|----|----|
| — | *（未取り込み）* | — | — | — | — | — |

> **初回の積み方**: `/loop-review` を回し、機能一覧の依存順（User → Household → Membership → Invitation → Category → UserCategoryPreference → Entry → Budget → ChatMessage）を踏まえて、**直近で着手する数件だけ**を取り込む。優先度は人間が決める。
> **種別**: `API`（`/impl-api` で消化） / `画面`（`/impl-front` で消化） / `設計`（`/review-design` `/generate-api-spec` `/generate-er` 等） / `基盤`（CI・環境・共通コード）

---

## 要望キュー

*利用者（パートナー・本人）から出た声。**誰の・どんな言葉か**を引用で残す。リリース後はここが主戦場になる。*

| ID | 要望（誰の・言葉） | 分類 | 優先 | ステータス | ゲート | 設計 |
|----|----|----|----|----|----|----|
| — | *（受領なし）* | — | — | — | — | — |

---

## 技術的ギャップ（AI 側で気づいた点・要望ではない）

*利用者から出た「要望」ではなく、コードや設計を見て見つかった弱い所。**要望キューと混ぜない**（優先度の性質が違う）。*

| ID | 内容 | 影響 | 優先 |
|----|----|----|----|
| — | *（未記載）* | — | — |

---

## 完了ログ

*merged になった項目をここへ移す。PR 番号と日付を残す。*

| ID | 対象 | PR | merged |
|----|----|----|----|
| — | — | — | — |

---

## 見送り

*やらないと決めたもの。**理由を必ず残す**（同じ議論を繰り返さないため）。*

| ID | 対象 | 見送り理由 | 判断日 |
|----|----|----|----|
| — | — | — | — |
